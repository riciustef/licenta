@model IEnumerable<StefanRiciu.Models.Traseu>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Denumire)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Denumire)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.TraseuID">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.TraseuID">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.TraseuID">Delete</a>
        </td>
    </tr>
}
</table>
